# Tests

This directory is reserved for future tests.

Use synthetic or sanitized fixtures only. Tests should verify that extraction code:

- preserves source references;
- does not store important facts without citations;
- flags high-impact timeline/contract changes for human review;
- handles duplicate files and email threads safely;
- avoids treating old revisions as current without evidence.
